import React, {Fragment} from "react";
import NavBar from "../NavBar/navbarComponent";

const HomePage = () => {
    return (
        <Fragment>
            <NavBar />
        </Fragment>    
    );
};

export default HomePage;
